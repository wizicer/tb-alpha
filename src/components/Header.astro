---
import { getLocalizedPath, stripLangFromPath, languages } from "../i18n/translations";
import type { Lang } from "../i18n/translations";

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;

const navLabels: Record<Lang, Record<string, string>> = {
  ja: {
    home: "ホーム",
    services: "サービス",
    membership: "会員",
    company: "会社概要",
    contact: "お問い合わせ",
  },
  en: {
    home: "Home",
    services: "Services",
    membership: "Membership",
    company: "Company",
    contact: "Contact",
  },
  "zh-cn": {
    home: "首页",
    services: "服务",
    membership: "会员",
    company: "公司概要",
    contact: "联系我们",
  },
  "zh-tw": {
    home: "首頁",
    services: "服務",
    membership: "會員",
    company: "公司概要",
    contact: "聯繫我們",
  },
};

const navItems = [
  { label: navLabels[lang].home, path: "/" },
  { label: navLabels[lang].services, path: "/#services" },
  { label: navLabels[lang].membership, path: "/membership" },
  { label: navLabels[lang].company, path: "/about" },
  { label: navLabels[lang].contact, path: "/contact" },
];

const currentPath = Astro.url.pathname;
const basePath = stripLangFromPath(currentPath);
const languageOrder: Lang[] = ["ja", "en", "zh-cn", "zh-tw"];
const languageShortLabels: Record<Lang, string> = {
  ja: "日本語",
  en: "EN",
  "zh-cn": "简中",
  "zh-tw": "繁中",
};
---

<header class="fixed top-0 left-0 right-0 z-50 bg-white/90 dark:bg-gray-900/90 backdrop-blur-md border-b border-gray-200 dark:border-gray-700">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <!-- Logo -->
      <a href={getLocalizedPath("/", lang)} class="flex items-center">
        <img src="/images/trainbit-logo.png" alt="TrainBit" class="h-11 w-auto" />
      </a>

      <!-- Desktop Navigation -->
      <nav class="hidden md:flex items-center gap-6">
        {navItems.map((item) => {
          const href = getLocalizedPath(item.path, lang);
          const isActive = false;
          return (
            <a
              href={href}
              class:list={[
                "text-sm font-medium transition-colors hover:text-primary-500",
                isActive
                  ? "text-primary-500"
                  : "text-gray-600 dark:text-gray-300",
              ]}
            >
              {item.label}
            </a>
          );
        })}
      </nav>

      <!-- Controls -->
      <div class="flex items-center gap-3">
        <!-- Language Switcher -->
        <details class="relative group">
          <summary
            class="list-none cursor-pointer inline-flex items-center gap-1.5 rounded-full bg-gray-100 dark:bg-gray-800 px-2.5 sm:px-3 py-1.5 text-xs font-semibold text-gray-700 dark:text-gray-200 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors whitespace-nowrap"
            aria-label="Select language"
          >
            <span class="text-primary-500 hidden sm:inline">Lang</span>
            <span class="sm:hidden">{languageShortLabels[lang]}</span>
            <span class="hidden sm:inline">{languages[lang]}</span>
            <svg class="w-3 h-3 text-gray-400 group-open:rotate-180 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </summary>
          <div class="absolute right-0 mt-2 w-36 overflow-hidden rounded-lg border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-900 shadow-lg">
            {languageOrder.map((targetLang) => {
              const label = languages[targetLang];
              const isCurrentLang = targetLang === lang;
              const localizedPath = getLocalizedPath(basePath, targetLang);
              const switchPath = `${localizedPath}${localizedPath.includes("?") ? "&" : "?"}lang=${targetLang}`;
              return (
                <a
                  href={switchPath}
                  data-lang={targetLang}
                  class:list={[
                    "block px-3 py-2 text-sm transition-colors",
                    isCurrentLang
                      ? "bg-primary-50 dark:bg-primary-900/30 text-primary-600 dark:text-primary-300 font-semibold"
                      : "text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-800",
                  ]}
                >
                  {label}
                </a>
              );
            })}
          </div>
        </details>

        <!-- Theme Toggle -->
        <button
          id="theme-toggle"
          class="p-2 rounded-full text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors"
          aria-label="Toggle theme"
        >
          <!-- Sun icon (shown in dark mode) -->
          <svg class="w-5 h-5 hidden dark:block" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
          </svg>
          <!-- Moon icon (shown in light mode) -->
          <svg class="w-5 h-5 block dark:hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
          </svg>
        </button>

        <!-- Mobile Menu Button -->
        <button
          id="mobile-menu-btn"
          class="md:hidden p-2 rounded-lg text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors"
          aria-label="Open menu"
        >
          <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile Navigation -->
  <div id="mobile-menu" class="hidden md:hidden border-t border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-900">
    <nav class="px-4 py-3 space-y-1">
      {navItems.map((item) => {
        const href = getLocalizedPath(item.path, lang);
        const isActive = false;
        return (
          <a
            href={href}
            class:list={[
              "block px-3 py-2 rounded-lg text-sm font-medium transition-colors",
              isActive
                ? "bg-primary-50 dark:bg-primary-900/20 text-primary-500"
                : "text-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-800",
            ]}
          >
            {item.label}
          </a>
        );
      })}
    </nav>
  </div>
</header>

<script is:inline>
  // Theme toggle
  document.getElementById("theme-toggle")?.addEventListener("click", () => {
    const isDark = document.documentElement.classList.toggle("dark");
    localStorage.setItem("theme", isDark ? "dark" : "light");
  });

  // Mobile menu toggle
  document.getElementById("mobile-menu-btn")?.addEventListener("click", () => {
    document.getElementById("mobile-menu")?.classList.toggle("hidden");
  });
</script>
